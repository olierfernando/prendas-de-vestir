<%= simple_form_for @talla, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :medida_id %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                tallas_path, :class => 'btn' %>
  </div>
<% end %>

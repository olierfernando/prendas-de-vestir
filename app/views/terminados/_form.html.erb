<%= simple_form_for @terminado, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :bordados %>
  <%= f.input :estampado %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                terminados_path, :class => 'btn' %>
  </div>
<% end %>
